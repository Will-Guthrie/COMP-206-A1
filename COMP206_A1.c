#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]) {
    if (argc != 4)
    {
        printf("Please submit exactly three arguments");
        exit(-1);
    }

    FILE* shapeTemplate = fopen(argv[1], "r");
    if (shapeTemplate = NULL) {

    }

}
